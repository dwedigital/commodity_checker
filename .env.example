# Commodity Checker Environment Variables

# Domain for inbound emails (e.g., inbound.yourdomain.com)
INBOUND_EMAIL_DOMAIN=inbound.example.com

# Mailgun credentials (for inbound email processing)
MAILGUN_API_KEY=your-mailgun-api-key
MAILGUN_INGRESS_SIGNING_KEY=your-mailgun-webhook-signing-key

# Anthropic Claude API (for commodity code suggestions)
ANTHROPIC_API_KEY=your-anthropic-api-key

# Rails (production)
RAILS_ENV=production
RAILS_LOG_LEVEL=info
SECRET_KEY_BASE=generate-with-bin/rails-secret

# Database (production - if using PostgreSQL)
# DATABASE_URL=postgres://user:password@host:5432/database
