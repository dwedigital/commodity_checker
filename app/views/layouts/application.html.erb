<!DOCTYPE html>
<html class="h-full bg-white">
  <head>
    <title><%= content_for(:title) || "Tariffik - never guess a code again" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <meta name="description" content="Forward your order confirmation emails and get accurate EU/UK commodity tariff codes suggested automatically using AI.">

    <!-- Open Graph -->
    <meta property="og:title" content="Tariffik - never guess a code again">
    <meta property="og:description" content="Forward your order confirmation emails and get accurate EU/UK commodity tariff codes suggested automatically using AI.">
    <meta property="og:image" content="<%= request.base_url %>/images/paperwork.png">
    <meta property="og:type" content="website">
    <meta property="og:url" content="<%= request.original_url %>">

    <!-- Twitter Card -->
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:title" content="Tariffik - never guess a code again">
    <meta name="twitter:description" content="Forward your order confirmation emails and get accurate EU/UK commodity tariff codes suggested automatically using AI.">
    <meta name="twitter:image" content="<%= request.base_url %>/images/paperwork.png">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700&family=Space+Grotesk:wght@400;500;600;700&display=swap" rel="stylesheet">

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="h-full">
    <div class="min-h-screen flex flex-col">
      <!-- Navigation -->
      <nav class="bg-white border-b border-gray-200">
        <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
          <div class="flex h-16 items-center justify-between">
            <div class="flex items-center">
              <div class="shrink-0">
                <%= link_to root_path, class: "text-brand-dark font-bold text-2xl tracking-tight" do %>
                  TARIFFIK
                <% end %>
              </div>
              <% if user_signed_in? %>
                <div class="hidden md:block">
                  <div class="ml-10 flex items-baseline space-x-1">
                    <%= link_to "Dashboard", dashboard_path, class: "px-4 py-2 text-sm font-medium transition-colors #{request.path == dashboard_path ? 'text-brand-dark bg-highlight rounded-full' : 'text-gray-600 hover:text-brand-dark'}" %>
                    <%= link_to "Lookup", product_lookups_path, class: "px-4 py-2 text-sm font-medium transition-colors #{request.path.start_with?('/dashboard/product_lookups') ? 'text-brand-dark bg-highlight rounded-full' : 'text-gray-600 hover:text-brand-dark'}" %>
                    <%= link_to "Orders", orders_path, class: "px-4 py-2 text-sm font-medium transition-colors #{request.path.start_with?('/dashboard/orders') ? 'text-brand-dark bg-highlight rounded-full' : 'text-gray-600 hover:text-brand-dark'}" %>
                    <%= link_to "API", developer_path, class: "px-4 py-2 text-sm font-medium transition-colors #{request.path.start_with?('/dashboard/developer') ? 'text-brand-dark bg-highlight rounded-full' : 'text-gray-600 hover:text-brand-dark'}" %>
                  </div>
                </div>
              <% end %>
            </div>
            <div class="hidden md:block">
              <div class="ml-4 flex items-center md:ml-6 gap-3">
                <% if user_signed_in? %>
                  <span class="text-gray-500 text-sm"><%= current_user.email %></span>
                  <%= button_to "Sign out", destroy_user_session_path, method: :delete, class: "rounded-full border border-gray-300 px-4 py-2 text-sm font-medium text-brand-dark hover:bg-gray-50 transition-colors cursor-pointer" %>
                <% else %>
                  <%= link_to "Sign in", new_user_session_path, class: "px-4 py-2 text-sm font-medium text-gray-600 hover:text-brand-dark transition-colors" %>
                  <%= link_to "Sign up", new_user_registration_path, class: "rounded-full bg-primary px-5 py-2 text-sm font-semibold text-white hover:bg-primary-hover transition-colors" %>
                <% end %>
              </div>
            </div>
          </div>
        </div>

        <!-- Mobile menu using native details/summary (no JS required) -->
        <details class="md:hidden group">
          <summary class="absolute top-3 right-4 list-none cursor-pointer inline-flex items-center justify-center rounded-full p-2 text-brand-dark hover:bg-gray-100 focus:outline-none [&::-webkit-details-marker]:hidden">
            <span class="sr-only">Open main menu</span>
            <!-- Hamburger icon (shown when closed) -->
            <svg class="size-6 group-open:hidden" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" d="M3.75 6.75h16.5M3.75 12h16.5m-16.5 5.25h16.5" />
            </svg>
            <!-- Close icon (shown when open) -->
            <svg class="size-6 hidden group-open:block" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12" />
            </svg>
          </summary>
          <div class="space-y-1 px-4 pb-3 pt-2 border-t border-gray-200">
            <% if user_signed_in? %>
              <%= link_to "Dashboard", dashboard_path, class: "block px-3 py-2 text-base font-medium #{request.path == dashboard_path ? 'text-brand-dark bg-highlight rounded-full' : 'text-gray-600 hover:text-brand-dark'}" %>
              <%= link_to "Lookup", product_lookups_path, class: "block px-3 py-2 text-base font-medium #{request.path.start_with?('/dashboard/product_lookups') ? 'text-brand-dark bg-highlight rounded-full' : 'text-gray-600 hover:text-brand-dark'}" %>
              <%= link_to "Orders", orders_path, class: "block px-3 py-2 text-base font-medium #{request.path.start_with?('/dashboard/orders') ? 'text-brand-dark bg-highlight rounded-full' : 'text-gray-600 hover:text-brand-dark'}" %>
              <%= link_to "API", developer_path, class: "block px-3 py-2 text-base font-medium #{request.path.start_with?('/dashboard/developer') ? 'text-brand-dark bg-highlight rounded-full' : 'text-gray-600 hover:text-brand-dark'}" %>
            <% end %>
          </div>
          <div class="border-t border-gray-200 pb-3 pt-4 px-4">
            <% if user_signed_in? %>
              <div class="mb-3">
                <div class="text-sm text-gray-500 truncate"><%= current_user.email %></div>
              </div>
              <%= button_to "Sign out", destroy_user_session_path, method: :delete, class: "w-full rounded-full border border-gray-300 px-4 py-2 text-sm font-medium text-brand-dark hover:bg-gray-50 transition-colors" %>
            <% else %>
              <div class="space-y-2">
                <%= link_to "Sign in", new_user_session_path, class: "block text-center px-4 py-2 text-base font-medium text-gray-600 hover:text-brand-dark" %>
                <%= link_to "Sign up", new_user_registration_path, class: "block text-center rounded-full bg-primary px-4 py-2 text-base font-semibold text-white hover:bg-primary-hover transition-colors" %>
              </div>
            <% end %>
          </div>
        </details>
      </nav>

      <!-- Flash messages (data-turbo-cache="false" prevents stale flashes with Turbo) -->
      <% if notice.present? %>
        <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 mt-4" data-turbo-cache="false">
          <div class="rounded-full border border-brand-mint bg-brand-mint/20 px-4 py-3">
            <div class="flex items-center gap-3">
              <span class="text-brand-mint">✓</span>
              <p class="text-sm font-medium text-brand-dark"><%= notice %></p>
            </div>
          </div>
        </div>
      <% end %>

      <% if alert.present? %>
        <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 mt-4" data-turbo-cache="false">
          <div class="rounded-full border border-primary bg-primary/10 px-4 py-3">
            <div class="flex items-center gap-3">
              <span class="text-primary">✗</span>
              <p class="text-sm font-medium text-brand-dark"><%= alert %></p>
            </div>
          </div>
        </div>
      <% end %>

      <!-- Main content -->
      <main class="grow">
        <% if content_for?(:full_width) %>
          <%= yield %>
        <% else %>
          <div class="mx-auto max-w-7xl px-4 py-6 sm:px-6 lg:px-8">
            <%= yield %>
          </div>
        <% end %>
      </main>

      <!-- Footer -->
      <%= render "shared/footer" %>
    </div>
  </body>
</html>
