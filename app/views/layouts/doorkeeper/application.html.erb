<%# Doorkeeper's controllers ask for their own layout. Render Tariffik's instead,
    so the consent and error screens carry the site nav, footer and styling
    rather than the gem's bundled stylesheet. %>
<%= render template: "layouts/application" %>
