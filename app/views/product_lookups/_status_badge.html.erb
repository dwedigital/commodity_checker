<%# Root carries the id so Turbo broadcasts can replace it repeatedly. %>
<%= lookup_status_badge(product_lookup, id: "product_lookup_status_#{product_lookup.id}") || tag.span(id: "product_lookup_status_#{product_lookup.id}") %>
