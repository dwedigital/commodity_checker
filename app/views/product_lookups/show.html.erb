<%= turbo_stream_from "product_lookup_#{@product_lookup.id}" %>

<div class="max-w-3xl mx-auto">
  <div class="md:flex md:items-center md:justify-between mb-8">
    <div class="min-w-0 flex-1">
      <nav class="flex mb-4" aria-label="Breadcrumb">
        <ol role="list" class="flex items-center space-x-2">
          <li>
            <%= link_to "Product Lookups", product_lookups_path, class: "text-sm font-medium text-gray-500 hover:text-brand-dark transition-colors" %>
          </li>
          <li class="flex items-center">
            <svg class="size-5 shrink-0 text-gray-400" viewBox="0 0 20 20" fill="currentColor">
              <path fill-rule="evenodd" d="M8.22 5.22a.75.75 0 0 1 1.06 0l4.25 4.25a.75.75 0 0 1 0 1.06l-4.25 4.25a.75.75 0 0 1-1.06-1.06L11.94 10 8.22 6.28a.75.75 0 0 1 0-1.06Z" clip-rule="evenodd" />
            </svg>
            <span class="ml-2 text-sm font-medium text-gray-500">Lookup</span>
          </li>
        </ol>
      </nav>
      <h2 class="text-2xl font-bold leading-7 text-brand-dark sm:truncate sm:text-3xl sm:tracking-tight">
        <%= @product_lookup.title.presence || "Product Lookup" %>
      </h2>
      <% if @product_lookup.retailer_name.present? %>
        <p class="mt-1 text-sm text-gray-500"><%= @product_lookup.retailer_name.titleize %></p>
      <% end %>
    </div>
    <div id="product_lookup_status_<%= @product_lookup.id %>" class="mt-4 flex md:ml-4 md:mt-0">
      <%= render "product_lookups/status_badge", product_lookup: @product_lookup %>
    </div>
  </div>

  <div id="product_lookup_<%= @product_lookup.id %>">
    <%= render "product_lookup", product_lookup: @product_lookup %>
  </div>
</div>
